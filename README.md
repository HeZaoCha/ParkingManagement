# 早点喝茶停车场管理系统

一个基于 Django 5.2 和 Python 3.13 的现代化停车场管理系统，支持多角色权限管理、VIP/员工免费停车、实时车位状态监控等功能。

**技术栈**: Python 3.13, Django 5.2, 充分利用Python 3.13新特性（内置类型注解、match/case、性能优化）

## 功能特性

### 核心功能
- ✅ 车辆入场/出场管理
- ✅ 实时车位状态监控
- ✅ 停车费用自动计算（支持固定收费和阶梯制收费）
- ✅ 支持中国车牌号验证（GA 36-2018标准）
- ✅ VIP/员工免费停车功能
- ✅ 数据统计仪表盘
- ✅ 阶梯收费系统（费率模板、自定义规则）
- ✅ 停车场类型管理（露天、立体、街道、地下）
- ✅ 停车场楼层和区域管理（多楼层、多区域划分）
- ✅ 车位号批量创建（简单创建、范围创建、文件上传）
- ✅ 车位批量创建结果详情（成功/跳过/失败列表，详细结果模态框）
- ✅ 费率预览功能（实时计算费用和明细，支持固定和阶梯收费）
- ✅ 排班表可视化（表格视图、周视图、统计视图）
- ✅ 用户注册功能（邮箱/手机验证码）
- ✅ 忘记密码功能（邮箱验证码重置，邮箱后缀自动补全，密码强度要求）
- ✅ 联系功能（反馈、在岗人员查询）
- ✅ 排班管理（Excel导入导出）

### 用户角色
- 👨‍💼 **管理员**：完全权限，系统配置
- 👷 **工作人员**：入场、出场、查询操作
- 🚗 **客户**：自助查询停车状态

### 界面
- 📱 **响应式设计**：完美支持移动端、平板、桌面端
- 🎨 **现代化UI**：11种主题色，支持亮暗模式切换
- ⚡ **快捷操作入口**：车辆入场/出场/查询一键操作
- 📊 **实时数据展示**：动态更新，无需刷新
- ✨ **增强交互性**：所有组件都有悬停、点击反馈
- 🎭 **流畅动画**：模态框、按钮、卡片都有平滑动画效果
- 🎯 **统一设计语言**：一致的视觉风格和交互体验

## 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Python | 3.13+ | 编程语言 |
| Django | 5.2.8 | Web框架 |
| SQLite | - | 开发数据库 |
| Tailwind CSS | 3.x | 前端样式 |
| Font Awesome | 6.4 | 图标库 |
| pytest | 9.0+ | 测试框架 |
| uv | - | 依赖管理 |

## 项目结构

本项目采用企业级Django项目结构：

```
ParkingManagement/
├── config/              # 项目配置（多环境支持）
│   ├── settings/        # 环境配置（base, dev, prod, test）
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── apps/                # 应用模块
│   ├── common/         # 通用模块
│   ├── infrastructure/ # 基础设施模块
│   ├── audit/          # 审计日志模块
│   └── ...
├── core/                # 全局基础代码（待完善）
├── infra/               # 基础设施代码（待完善）
├── parking/             # 核心业务模块
│   ├── services/       # 服务层（模块化）
│   │   ├── __init__.py
│   │   ├── parking_record_service.py
│   │   ├── vehicle_service.py
│   │   └── ...
├── scripts/             # 项目脚本
├── docs/                # 文档
│   ├── api/            # API文档
│   ├── deploy/         # 部署文档
│   └── architecture/   # 架构文档
├── Dockerfile          # Docker配置
└── docker-compose.yml  # Docker Compose配置
```

## 快速开始

### 1. 安装依赖

```bash
# 克隆项目
git clone <repository-url>
cd ParkingManagement

# 使用 uv 安装依赖
uv sync
```

### 2. 配置环境

项目支持多环境配置，默认使用开发环境：

```bash
# 开发环境（默认）
export DJANGO_SETTINGS_MODULE=config.settings.dev

# 或直接指定
uv run python manage.py runserver --settings=config.settings.dev
```

### 3. 初始化数据库

```bash
# 运行数据库迁移
uv run python manage.py migrate --settings=config.settings.dev

# 初始化测试数据（可选）
uv run python manage.py init_test_data --clear --settings=config.settings.dev
```

### 4. 启动服务

```bash
# 启动开发服务器
uv run python manage.py runserver 0.0.0.0:8000 --settings=config.settings.dev
```

### 5. 访问系统

| 页面 | URL | 说明 |
|------|-----|------|
| 登录页 | http://localhost:8000/login/ | 工作人员/管理员登录 |
| 工作台 | http://localhost:8000/dashboard/ | 日常操作界面 |
| 管理后台 | http://localhost:8000/manage/ | 数据管理 |
| 客户端 | http://localhost:8000/parking/customer/ | 客户自助查询 |

### 6. 测试账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 工作人员 | staff | staff123 |
| 客户 | customer | customer123 |

## 详细项目结构

```
ParkingManagement/
├── config/                      # 项目配置（企业级结构）
│   ├── settings/                # 多环境配置
│   │   ├── base.py             # 基础配置（共享）
│   │   ├── dev.py              # 开发环境
│   │   ├── prod.py             # 生产环境
│   │   └── test.py             # 测试环境
│   ├── urls.py                 # 主URL配置
│   ├── wsgi.py                 # WSGI配置
│   └── asgi.py                 # ASGI配置
├── core/                        # 全局基础代码（待完善）
│   ├── utils/                  # 全局工具函数
│   ├── middleware/             # 全局中间件
│   ├── exceptions/             # 全局异常类
│   ├── authentication/         # 认证相关
│   ├── pagination/             # 分页相关
│   └── permissions/           # 权限相关
├── infra/                       # 基础设施代码（待完善）
│   ├── cache/                  # 缓存配置
│   ├── logging/                # 日志配置
│   ├── celery/                 # Celery配置
│   ├── db/                     # 数据库配置
│   └── api_clients/            # 外部API客户端
├── apps/                        # 扩展应用模块
│   ├── audit/                  # 审计日志
│   ├── common/                 # 通用组件
│   ├── infrastructure/         # 基础设施模块
│   ├── config/                 # 系统配置
│   ├── notifications/          # 通知服务
│   └── reports/                # 报表服务
├── parking/                     # 核心业务模块
│   ├── models/                 # 数据模型（模块化）
│   │   ├── parking_lot.py
│   │   ├── parking_record.py
│   │   └── ...
│   ├── views/                  # 视图层（模块化）
│   │   ├── auth.py
│   │   ├── dashboard.py
│   │   └── ...
│   ├── services/               # 服务层（模块化）
│   │   ├── __init__.py
│   │   ├── parking_record_service.py
│   │   ├── vehicle_service.py
│   │   └── ...
│   ├── forms.py                # 表单验证
│   ├── urls.py                 # URL路由
│   ├── tests/                  # 测试用例
│   └── management/commands/    # 管理命令
├── templates/                   # 模板文件
│   ├── admin/                  # 管理后台模板
│   ├── customer/               # 客户端模板
│   ├── components/             # 可复用组件
│   └── *.html                  # 页面模板
├── docs/                       # 项目文档
├── tests/                      # 集成测试
└── pyproject.toml              # 项目依赖配置
```

## 数据模型

### ParkingLot（停车场）
| 字段 | 类型 | 说明 |
|------|------|------|
| name | CharField | 停车场名称 |
| address | CharField | 详细地址 |
| lot_type | CharField | 停车场类型（露天/立体/街道/地下） |
| floors | JSONField | 楼层列表（如["B2", "B3", "1F"]） |
| areas | JSONField | 区域信息（如{"B2": ["A区", "B区"]}） |
| total_spaces | Integer | 总车位数 |
| hourly_rate | Decimal | 每小时费率（元，固定收费模式） |
| is_active | Boolean | 是否启用 |

### PricingTemplate（费率模板）
| 字段 | 类型 | 说明 |
|------|------|------|
| name | CharField | 模板名称 |
| free_minutes | Integer | 免费时长（分钟） |
| daily_max_fee | Decimal | 每日收费上限（元） |
| is_active | Boolean | 是否启用 |

### PricingRule（费率规则）
| 字段 | 类型 | 说明 |
|------|------|------|
| template | ForeignKey | 所属模板 |
| start_minutes | Integer | 起始分钟 |
| end_minutes | Integer | 结束分钟（可选） |
| rate_per_hour | Decimal | 每小时费率（元） |
| order | Integer | 排序顺序 |

### ParkingSpace（停车位）
| 字段 | 类型 | 说明 |
|------|------|------|
| parking_lot | ForeignKey | 所属停车场 |
| floor | CharField | 楼层（如"B2"、"1F"） |
| area | CharField | 区域（如"A区"、"B区"） |
| space_number | CharField | 车位编号 |
| space_type | CharField | 类型（标准/VIP/大型/残疾人） |
| is_occupied | Boolean | 是否占用 |
| is_reserved | Boolean | 是否预留 |

### Vehicle（车辆）
| 字段 | 类型 | 说明 |
|------|------|------|
| license_plate | CharField | 车牌号（唯一） |
| vehicle_type | CharField | 车辆类型 |
| owner_name | CharField | 车主姓名 |
| owner_phone | CharField | 联系电话 |

### VIPVehicle（VIP/免费车辆）
| 字段 | 类型 | 说明 |
|------|------|------|
| license_plate | CharField | 车牌号 |
| vip_type | CharField | 类型（员工/VIP/合作伙伴） |
| discount_rate | Decimal | 折扣率（1.00=免费） |
| valid_from | Date | 生效日期 |
| valid_until | Date | 失效日期（空=永久） |

### ParkingRecord（停车记录）
| 字段 | 类型 | 说明 |
|------|------|------|
| vehicle | ForeignKey | 车辆 |
| parking_space | ForeignKey | 停车位 |
| entry_time | DateTime | 入场时间 |
| exit_time | DateTime | 出场时间 |
| fee | Decimal | 停车费用 |
| is_paid | Boolean | 是否已支付 |
| is_free_parking | Boolean | 是否免费 |

## API文档

详细API文档请参阅 [docs/API.md](docs/API.md)

### 主要接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/parking/api/entry/` | POST | 车辆入场 |
| `/parking/api/exit/` | POST | 车辆出场 |
| `/parking/api/query/` | GET | 车辆查询 |
| `/parking/api/stats/` | GET | 统计数据 |
| `/parking/api/lots/` | GET | 停车场列表 |
| `/parking/api/validate-plate/` | GET | 车牌验证 |

## Docker部署

### 使用Docker Compose（推荐）

```bash
# 启动所有服务（数据库、Redis、Web应用）
docker-compose up -d

# 查看日志
docker-compose logs -f web

# 执行数据库迁移
docker-compose exec web uv run python manage.py migrate --settings=config.settings.prod

# 停止服务
docker-compose down
```

### 生产环境部署

1. **设置环境变量**：
```bash
export SECRET_KEY=your-secret-key
export ALLOWED_HOSTS=your-domain.com
export DB_NAME=parking_management
export DB_USER=postgres
export DB_PASSWORD=your-password
export DB_HOST=db
export REDIS_URL=redis://redis:6379/1
```

2. **使用生产环境配置**：
```bash
export DJANGO_SETTINGS_MODULE=config.settings.prod
```

3. **运行迁移和收集静态文件**：
```bash
uv run python manage.py migrate --settings=config.settings.prod
uv run python manage.py collectstatic --noinput --settings=config.settings.prod
```

## 测试

```bash
# 运行所有测试
uv run pytest parking/tests/ -v

# 运行特定测试
uv run pytest parking/tests/test_services.py -v

# 查看测试覆盖率
uv run pytest --cov=parking parking/tests/

# 使用测试环境配置
uv run pytest --settings=config.settings.test
```

当前测试覆盖：
- 模型测试：27个用例
- 表单测试：26个用例
- 服务测试：49个用例
- API测试：15个用例
- **总计：172个测试用例全部通过**

## 开发规范

- **Python**: 遵循 PEP 8 和 PEP 257
- **Django**: 遵循 Django 最佳实践
- **Git**: 遵循 Conventional Commits 规范
- **测试**: 使用 pytest + factory_boy

## 车牌号规范

本系统支持中国大陆车牌号（GA 36-2018标准）：

- 普通车牌：省份+地市+5位（如：粤E9KM03）
- 新能源车牌：省份+地市+D/F+5位（如：粤ED12345）
- 特殊车牌：挂车、学车、警车、港澳车牌

**注意**：地级市代号不含字母I和O（避免与数字1、0混淆）

## 主要功能说明

### 阶梯收费系统

支持灵活的阶梯制收费，可设置：
- 免费时长（15分钟、30分钟等）
- 多时间段不同费率（如：15-60分钟5元/小时，60-180分钟4元/小时）
- 每日收费上限
- 费率模板保存和复用

### 停车场类型

- **露天停车场**：单层，无楼层信息
- **立体停车楼**：多层，每层可有多个区域
- **街道停车场**：单层，多个不同区域
- **地下停车场**：多层，每层可有多个区域

### 车位号批量创建

支持三种方式创建车位：
1. **范围创建**：输入A001-A1000，自动生成1000个车位
2. **文档上传**：上传txt/md/Excel文件，自动解析车位号
3. **模板下载**：下载Excel模板，填写后上传

## 更新日志

详见 [CHANGELOG.md](CHANGELOG.md)

当前版本：**v1.1.0** (2025-12-11)

## 许可证

MIT License - 详见 [LICENSE](LICENSE)
